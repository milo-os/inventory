---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: nodetypes.graph.inventory.miloapis.com
spec:
  group: graph.inventory.miloapis.com
  names:
    kind: NodeType
    listKind: NodeTypeList
    plural: nodetypes
    singular: nodetype
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Display
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          NodeType describes the attribute schema for a class of graph Nodes. It is
          the source of truth the admission webhook validates Node.spec.attributes
          against. See RFC milo-os/inventory#43.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NodeTypeSpec describes the closed attribute schema for Nodes whose
              spec.type equals this NodeType's metadata.name.
            properties:
              attributes:
                description: |-
                  Attributes is the closed set of attribute keys a Node of this type may
                  carry. Keys outside this set are rejected at admission; keys marked
                  Required must be present.
                items:
                  description: |-
                    AttributeSchema describes one allowed attribute key on a Node or Edge type.
                    The set of AttributeSchemas on a NodeType/EdgeType is the closed schema the
                    admission webhook validates each object's attributes against.
                  properties:
                    enum:
                      description: Enum optionally restricts a String attribute to
                        a fixed set of values.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    key:
                      description: Key is the attribute name (the map key in spec.attributes).
                      minLength: 1
                      type: string
                    required:
                      description: |-
                        Required marks the attribute as mandatory. Objects missing a required
                        attribute are rejected at admission.
                      type: boolean
                    type:
                      description: Type is the scalar kind the value must parse as.
                      enum:
                      - String
                      - Integer
                      - Float
                      - Boolean
                      type: string
                  required:
                  - key
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - key
                x-kubernetes-list-type: map
              displayName:
                description: DisplayName is a human-readable label for the node type.
                type: string
            type: object
          status:
            default:
              conditions:
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for reconciliation
                reason: Pending
                status: "False"
                type: Ready
            description: NodeTypeStatus defines the observed state of a NodeType.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
