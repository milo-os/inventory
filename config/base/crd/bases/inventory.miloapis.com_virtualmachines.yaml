---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: virtualmachines.inventory.miloapis.com
spec:
  group: inventory.miloapis.com
  names:
    kind: VirtualMachine
    listKind: VirtualMachineList
    plural: virtualmachines
    shortNames:
    - vm
    singular: virtualmachine
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.hostRef.name
      name: Host
      type: string
    - jsonPath: .spec.allocation.vcpus
      name: vCPUs
      type: integer
    - jsonPath: .spec.allocation.memoryBytes
      name: Memory
      type: integer
    - jsonPath: .spec.providerRef.name
      name: Provider
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VirtualMachine is the Schema for the virtualmachines API. A VirtualMachine
          runs on a host Node and records its compute allocation and optional
          provider/project association.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              VirtualMachineSpec defines the desired state of VirtualMachine. It models
              host assignment and allocation only; power state and health are explicit
              non-goals.
            properties:
              allocation:
                description: Allocation describes the compute resources allocated
                  to the VM.
                properties:
                  disks:
                    description: |-
                      Disks is the list of virtual disks allocated to the VM. The disk shape
                      is shared with Node.
                    items:
                      description: NodeDisk describes a single disk attached to a
                        Node.
                      properties:
                        name:
                          description: Name identifies the disk (e.g. device name
                            or asset label).
                          minLength: 1
                          type: string
                        sizeBytes:
                          description: SizeBytes is the total raw capacity of the
                            disk in bytes.
                          format: int64
                          minimum: 1
                          type: integer
                        type:
                          description: Type classifies the disk media.
                          enum:
                          - SSD
                          - HDD
                          - NVMe
                          type: string
                      required:
                      - name
                      - sizeBytes
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  memoryBytes:
                    description: MemoryBytes is the amount of RAM allocated, in bytes.
                    format: int64
                    minimum: 1
                    type: integer
                  vcpus:
                    description: VCPUs is the number of virtual CPUs allocated.
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - memoryBytes
                - vcpus
                type: object
              hostRef:
                description: |-
                  HostRef references the Node this VM runs on. This field is immutable
                  after creation.
                properties:
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              projectRef:
                description: |-
                  ProjectRef optionally links this VM to a resourcemanager Project (or
                  other platform resource) in another API group.
                properties:
                  apiGroup:
                    description: APIGroup of the referenced resource (e.g. "networking.miloapis.com").
                    minLength: 1
                    type: string
                  kind:
                    description: Kind of the referenced resource.
                    minLength: 1
                    type: string
                  name:
                    description: Name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the referenced resource, for namespaced
                      targets.
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              providerRef:
                description: ProviderRef optionally references the Provider that owns
                  this VM.
                properties:
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - allocation
            - hostRef
            type: object
            x-kubernetes-validations:
            - message: hostRef is immutable
              rule: self.hostRef == oldSelf.hostRef
          status:
            default:
              conditions:
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for reconciliation
                reason: Pending
                status: "False"
                type: Ready
            description: VirtualMachineStatus defines the observed state of VirtualMachine.
            properties:
              conditions:
                description: |-
                  Represents the observations of a virtual machine's current state.
                  Known condition types are: "Ready".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
